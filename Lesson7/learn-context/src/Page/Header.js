import React from 'react';

const Header = (props) => {
  return (
    <div style={{ width: '100%', height: '200px', background: props.color }}>
      Header
    </div>
  );
};

export default Header;
