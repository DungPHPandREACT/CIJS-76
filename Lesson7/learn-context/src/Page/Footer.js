import React from 'react';

const Footer = (props) => {
  return (
    <div style={{ width: '100%', height: '200px', background: props.color }}>
      Footer
    </div>
  );
};

export default Footer;
